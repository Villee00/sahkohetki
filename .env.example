# Sähköhetki environment configuration

# Enable mock data mode (recommended for local development and AI agents without API keys)
SAHKO_MOCK_DATA=1

# ENTSO-E Transparency Platform API token (only required if SAHKO_MOCK_DATA is unset or "0")
# ENTSOE_TOKEN=your-entsoe-token-here
